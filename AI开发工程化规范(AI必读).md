# AI 开发工程化规范（AI 必读）

> 本文件是本仓库所有 AI 开发任务的最高优先级约束。任何开发、修改、重构都必须遵守以下规范。

## 1. 所有需求都需要先搜索一遍行内实现方案 / EXPO 规范

- 动手写代码前，必须先检索本仓库已有的实现方案，以及 Expo / React Native 的官方规范与最佳实践。
- 存在现成方案时优先复用，不得凭空发明；确认无现成方案后再设计新的实现。
- 技术选型、API 用法以 Expo 官方文档与当前安装版本为准，不得使用过时或已废弃的写法。

## 2. 所有实现必须优雅简单

- 代码追求简单、清晰、可读，避免过度设计、过度抽象和不必要的复杂度。
- 能用几行解决的问题不写几十行；不做"看起来高级"但没有实际价值的封装。
- 保持一致的命名、结构和注释风格，注释只解释"为什么"，不解释"是什么"。

## 3. 所有类似功能必须封装为组件

- 重复出现两次及以上的结构、逻辑、样式，必须抽取为可复用组件或公共函数，禁止复制粘贴。
- 组件保持单一职责，通过 props 控制差异；公共样式、颜色、尺寸统一收敛到 constants。
- 页面内的小差异用配置/参数表达，而不是派生出一堆几乎相同的代码块。

## 4. 所有 UI 样式和交互必须以可用为目的，不能写出 Demo 水平的代码

- UI 与交互按"真实可用的产品"标准实现：布局正确、状态完整、边界情况有处理、交互反馈即时。
- 禁止只搭骨架、写死数据、忽略点击态/加载态/空态/错误态等"演示级"实现。
- 完成的界面必须能被用户正常操作完整走通，并经实际运行验证后交付。
